public class spl {
    public static float[] inverse_Spl(float[][] matrix){
        int row = matrix.length;
        int col = matrix[0].length;
        for (int i=0;i<[]) {

        }
        float[][] inv = invers_identity.invert(matrix[0:row][0:col-1]);


    }

}

